--ranking used to give rank based on condition

1.rank()- it will skip next rank if two people have same rank
example:
select *, rank() over(order by marks desc) as 'rank' from students;

2.dense_rank()
select *, rank() over(order by marks desc) as 'rank', 
    dense_rank() over(order by marks desc) as 'dense_rank'from students;
select  *  dense_rank() over(order by marks desc) as 'dense_rank'from students;

--partitioning
select Name as country,continent,population, rank()
    over(partition by continent order by population desc) as rankwithcont
    from country;